# 🔒 คู่มือเพิ่มความปลอดภัย (Security Guide)
สำหรับ Web Project บน Render.com

เพื่อให้เว็บไซต์ของคุณปลอดภัยจากการถูกเจาะหรือแฮ็ก กรุณาทำตามขั้นตอนต่อไปนี้บนหน้า Dashboard ของ Render ครับ:

## 1. ไปที่แท็บ Environment Variables
ในหน้าโปรเจกต์ของคุณบน Render ให้ไปที่เมนู **Environment** แล้วเพิ่มค่าดังนี้:

### 🔑 ตั้งค่าความลับ (Required)
| Key | Value | คำอธิบาย |
|-----|-------|----------|
| `SECRET_KEY` | (สุ่มรหัสยาวๆ) | ใช้สร้างรหัสสุ่มในเว็บ ห้ามบอกใคร |
| `DEBUG` | `False` | **สำคัญมาก!** ปิดโหมดคนทำเว็บ เพื่อป้องกันข้อมูลรั่วไหล |
| `CLOUDCONVERT_API_KEY` | (คีย์ของคุณ) | ย้ายคีย์จาก Code มาเก็บตรงนี้แทน เพื่อความปลอดภัย |

> 💡 **วิธีสร้าง Secret Key:**
> เปิด Python ในเครื่องคุณแล้วพิมพ์:
> ```python
> import secrets; print(secrets.token_urlsafe(50))
> ```
> ก๊อปปี้รหัสที่ได้ไปใส่เลยครับ

---

## 2. สิ่งที่ระบบทำเพิ่มให้แล้ว (Auto Security)
เมื่อคุณตั้งค่า `DEBUG = False` ในข้อ 1 ระบบจะเปิดโหมดป้องกันตัวเองอัตโนมัติ ดังนี้:

- **🔒 Auto HTTPS:** บังคับให้ทุกคนเข้าเว็บผ่าน HTTPS เท่านั้น (ถ้าเข้า HTTP ธรรมดา จะถูกดีดไป HTTPS อัตโนมัติ)
- **🍪 Secure Cookies:** เข้ารหัสคุกกี้ทุกครั้ง ป้องกันการขโมย Session
- **🛡️ HSTS Header:** สั่งให้ Browser จำว่าเว็บนี้ต้องปลอดภัยเสมอ (ป้องกัน Man-in-the-middle attacks)
- **🤖 CSRF Protection:** ป้องกันการปลอมแปลงฟอร์มจากเว็บอื่น

## 3. คำแนะนำเพิ่มเติม
- **อย่าเก็บไฟล์ .env ไว้ใน Git:** (ในโปรเจกต์นี้เราไม่ได้สร้าง .env แต่ใช้ Environment Variable ของ Host โดยตรง ซึ่งปลอดภัยที่สุดแล้วครับ)
- **หมั่นเปลี่ยน Secret Key:** ถ้ารู้สึกว่ามีความเสี่ยง ให้เปลี่ยนค่า `SECRET_KEY` ใน Render ได้เลย (Note: ทุกคนต้อง Login ใหม่หลังเปลี่ยน)

---
✅ **สรุป:** แค่เพิ่ม Environment Variable ใน Render ตามข้อ 1 เว็บของคุณก็จะปลอดภัยระดับมาตรฐานสากลทันทีครับ!
