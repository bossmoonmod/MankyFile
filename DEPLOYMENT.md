# 🚀 การติดตั้งบน Ubuntu Server ด้วย CloudPanel

คู่มือนี้จะช่วยคุณติดตั้ง **MankyFile Project** บน Ubuntu Server โดยใช้ **CloudPanel** ซึ่งเป็น Control Panel ที่จัดการ Python App ได้ง่ายมาก

---

## 1. ติดตั้ง LibreOffice บน Server (สำคัญมาก)
เครื่องมือแปลงไฟล์ Word/PPT/PDF จำเป็นต้องใช้ LibreOffice แบบ Headless ติดตั้งผ่าน SSH:

```bash
sudo apt update
sudo apt install -y libreoffice --no-install-recommends
# ตรวจสอบการติดตั้ง
which libreoffice
# ผลลัพธ์ควรเป็น: /usr/bin/libreoffice
```

---

## 2. ขั้นตอนใน CloudPanel Dashboard
1. เข้าไปที่ CloudPanel ของคุณ
2. คลิก **Add Site** -> **Create a Python Site**
3. กรอกข้อมูล:
   - **Domain Name:** (เช่น pdf.yourdomain.com)
   - **Python Version:** เลือก 3.10 หรือใหม่กว่า
   - **Application Directory:** `/home/cloudpanel-user/htdocs/pdf.yourdomain.com`

---

## 3. เตรียมไฟล์โค้ด
คุณสามารถใช้ Git หรืออัปโหลดไฟล์ผ่าน FTP/File Manager:

```bash
# ตัวอย่างการใช้ Git ภายในโฟลเดอร์ Application Directory
cd /home/cloudpanel-user/htdocs/pdf.yourdomain.com
git clone https://github.com/your-repo/mankyfile.git .
```

---

## 4. ติดตั้ง Dependencies และสภาพแวดล้อม
CloudPanel จะสร้าง Virtual Environment ให้โดยอัตโนมัติ คุณต้องทำการติดตั้ง Requirements:

```bash
# เปิดใช้งาน Virtual Environment (ชื่อโฟลเดอร์ venv อาจแตกต่างกันตามเวอร์ชัน)
source venv/bin/activate

# ติดตั้งแพ็คเกจที่จำเป็น
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 5. ตั้งค่า Django (Settings.py)
แก้ไขไฟล์ `core/settings.py` สำหรับการใช้งานจริง:

```python
DEBUG = False
ALLOWED_HOSTS = ['pdf.yourdomain.com']

# ตรวจสอบ STATIC & MEDIA ROOT
STATIC_ROOT = BASE_DIR / "staticfiles"
MEDIA_ROOT = BASE_DIR / "media"
```

---

## 6. คำสั่งเริ่มต้นระบบ
รันคำสั่งเหล่านี้เพื่อให้ระบบพร้อมใช้งาน:

```bash
# สร้างฐานข้อมูล
python manage.py migrate

# รวบรวมไฟล์ Static (CSS, JS)
python manage.py collectstatic --noinput

# สร้างโฟลเดอร์สำหรับเก็บไฟล์
mkdir -p media/uploads media/processed
chmod -R 775 media/
```

---

## 7. ตั้งค่า Python App ใน CloudPanel
ในเมนู **Python Config** ของ CloudPanel ให้ตั้งค่าดังนี้:

- **Entry Point:** `core/wsgi.py` (หรือตามโครงสร้างโปรเจค)
- **Application Port:** (CloudPanel จะจัดการจัดการ Proxy ให้เอง)

ในส่วนของ **Vhost Content** (Nginx Config) ให้เพิ่มส่วนนี้เพื่อจัดการไฟล์ Media และ Static:

```nginx
# Static Files
location /static/ {
    alias /home/cloudpanel-user/htdocs/pdf.yourdomain.com/staticfiles/;
}

# Media Files (สำคัญสำหรับการดาวน์โหลดไฟล์ที่แปลงเสร็จ)
location /media/ {
    alias /home/cloudpanel-user/htdocs/pdf.yourdomain.com/media/;
    add_header Content-Disposition "attachment";
}
```

---

## 8. ตั้งค่าระบบลบไฟล์อัตโนมัติ (Cron Job)
เพื่อให้เซิร์ฟเวอร์ไม่เต็มจากการเก็บไฟล์ผู้ใช้ ให้ตั้ง Cron Job ใน CloudPanel:

1. ไปที่เมนู **Cron Jobs**
2. คลิก **Add Cron Job**
3. ตั้งค่ารันทุก 30 นาที:
   - **Command:** `cd /home/cloudpanel-user/htdocs/pdf.yourdomain.com && ./venv/bin/python manage.py cleanup_files --hours 1`
   - **Schedule:** `*/30 * * * *`

---

## 9. ทดสอบการใช้งาน
1. เข้าไปที่หน้าเว็บ
2. ทดลองอัปโหลดไฟล์ในโหมด "Word เป็น PDF"
3. หากติดปัญหา **"LibreOffice not found"** ให้ตรวจสอบ Path ใน `views.py` หรือตรวจสอบสิทธิ์การเข้าถึงไฟล์

---

## 💡 เคล็ดลับเพิ่มเติม
- **Permissions:** ตรวจสอบให้แน่ใจว่า user **clp-user** (หรือ user ของเว็บ) มีสิทธิ์เขียนไฟล์ในโฟลเดอร์ `media/`
- **Logs:** หากหน้าเว็บขึ้น Error 502 หรือ 500 ให้ดู log ที่:
  `/home/cloudpanel-user/logs/pdf.yourdomain.com/error.log`
