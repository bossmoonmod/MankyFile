# 🚀 MankyFile V.2: API-First Architecture
> **เป้าหมาย:** สร้างระบบแปลงไฟล์ที่ "รันได้ทุกที่" (Server-Agnostic) โดยไม่ต้องติดตั้งโปรแกรมเสริมบนเซิร์ฟเวอร์

---

## 💡 ทำไมต้อง V.2? (The Problem)
ใน V.1 เราใช้ **LibreOffice** ติดตั้งลงใน Server ซึ่งมีข้อจำกัด:
1.  **หา Host ยาก:** Shared Hosting ทั่วไปไม่อนุญาตให้ติดตั้งโปรแกรม .exe หรือ package เพิ่มเติม
2.  **หนักเครื่อง:** การแปลงไฟล์กิน Ram และ CPU สูงมาก ถ้าคนใช้เยอะ Server อาจล่ม
3.  **การดูแลรักษา:** ต้องคอยอัปเดต LibreOffice และแก้ปัญหา Font ภาษาไทยเพี้ยน

## 🛠️ แนวทางแก้ไข ( The Solution)
เปลี่ยนจาก "ทำเอง" เป็น "จ้างคนอื่นทำ" ด้วย **External APIs**:
*   **Web Server (Django):** ทำหน้าที่แค่ "รับไฟล์" และ "ส่งต่อ"
*   **Processing:** ปล่อยให้ API ของผู้ให้บริการจัดการ
*   **Result:** รับไฟล์ผลลัพธ์กลับมาแสดงให้ผู้ใช้

---

## 🔌 API Candidates (ตัวเลือกแนะนำ)

### 1. CloudConvert API (⭐ แนะนำ)
*   **จุดเด่น:** รองรับไฟล์ครอบจักรวาล (200+ formats), คุณภาพสูงมาก, มี Python SDK
*   **ราคา:** ฟรี 25 ไฟล์/วัน (หลังจากนั้นแบบเติมเงิน ราคาไม่แพง)
*   **ความง่าย:** มี Library `cloudconvert` สำหรับ Python ใช้ง่ายมาก

### 2. Adobe PDF Services API
*   **จุดเด่น:** เจ้าของ PDF ตัวจริง การแปลงแม่นยำที่สุด Format ไม่เพี้ยน
*   **ราคา:** มี Free Tier จำนวนหนึ่ง หลังจากนั้นคิดตาม Transaction
*   **ความง่าย:** เหมาะกับงานที่ต้องการความเป๊ะสูงๆ (Enterprise Grade)

### 3. ConvertAPI
*   **จุดเด่น:** ติดตั้งง่าย Document ดี
*   **ราคา:** ฟรีจำกัดวินาทีการแปลง

---

## 🗺️ Roadmap การพัฒนา V.2

### Phase 1: Preparation (เตรียมการ)
- [ ] สมัคร Account CloudConvert (เพื่อรับ API Key)
- [ ] ติดตั้ง Library: `pip install cloudconvert`
- [ ] สร้าง Configuration ใน `settings.py` สำหรับเก็บ CLOUDCONVERT_API_KEY

### Phase 2: Core Refactoring (รื้อระบบแปลง)
- [ ] **Word to PDF:** เปลี่ยนจาก `subprocess LibreOffice` -> `cloudconvert.Job.create()`
- [ ] **PDF to Word:** เปลี่ยน library เดิม -> `cloudconvert` (คุณภาพดีกว่าเดิมมาก)
- [ ] **PowerPoint/Excel:** เพิ่มฟีเจอร์นี้ได้ทันทีเพราะ API รองรับอยู่แล้ว

### Phase 3: Deployment Optimizations
- [ ] **Remove Binary Dependencies:** ลบโค้ดตรวจสอบ LibreOffice ออก
- [ ] **Cleanup Requirements:** ลบ Library หนักๆ ที่ไม่จำเป็นออก
- [ ] **Deploy Test:** ทดสอบบน Shared Hosting หรือ Cloud ฟรี (เช่น Render/Vercel) ว่าทำงานได้จริง

---

## 📝 ตัวอย่างโค้ด (Concept)

```python
import cloudconvert

def convert_word_to_pdf_v2(input_file_path):
    cloudconvert.configure(api_key = 'YOUR_API_KEY')
    
    # อัปโหลดและสั่งแปลง
    job = cloudconvert.Job.create(payload={
        "tasks": {
            "upload-file": {
                "operation": "import/upload"
            },
            "convert-file": {
                "operation": "convert",
                "input": "upload-file",
                "output_format": "pdf"
            },
            "export-file": {
                "operation": "export/url",
                "input": "convert-file"
            }
        }
    })
    
    # ... (Wait for processing) ...
    # Download file กลับมา
```

---

## ✅ สรุป
การย้ายไป V.2 จะทำให้ MankyFile กลายเป็นโปรเจกต์ที่ **Modern** และ **Scalable** มากขึ้น สามารถเปิดบริการจริงได้โดยไม่ต้องกังวลเรื่อง Server ล่มจากการแปลงไฟล์หนักๆ ครับ
